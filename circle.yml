machine:
  ruby:
    version:
      2.2.3

dependencies:
  pre:
    - bundle install
    - bundle exec jekyll build

test:
  pre:
    - bundle exec jekyll test
    - bundle exec htmlproof _site --disable-external
